name: Runs on pull request on staging branch

on:
  push:
    branches:
      - staging

jobs:
  call:
    uses: igdevx/ShopouilleBackend/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      dockerfile_path: src/main/docker/Dockerfile.jvm
